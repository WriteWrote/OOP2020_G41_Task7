package com.company;

import com.company.products.*;

public class Main {

    public static void main(String[] args) {
        // write your code here
        System.out.println("Hello world!");

/*
        Stock stock = new Stock();
        Uncountable milk = (Uncountable) ProductType.Milk.getDescription(100, 100);
        if (stock.getCapacity() - stock.get(milk.getName()) < milk.getWeight()) {
            stock.add(milk.getName(), stock.getCapacity());
        } else {
            int a = stock.getStock().get(milk.getName());
            stock.add(milk.getName(), a + milk.getWeight());
        }
        Description description = ProductType.Drink.getDescription(0, 5);
        int b = description.getQuantity();
        description = ProductType.Alcohol.getDescription(0, 2);
        b = description.getQuantity();*/
    }
}
